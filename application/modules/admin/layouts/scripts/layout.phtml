<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="utf-8"/>
        <?php
        $this->headLink()->appendStylesheet($this->baseUrl('/css/style.css'));
        $this->headLink()->appendStylesheet($this->baseUrl('/css/admin.css'));
        
        $this->headScript(Zend_View_Helper_HeadScript::FILE, $this->baseUrl('/js/prototype.js'));
        $this->headScript(Zend_View_Helper_HeadScript::FILE, $this->baseUrl('/js/lib/tools.js'));
        
        echo $this->headLink();
        echo $this->headScript();
        ?>
        <title>todo</title>
    </head>
    <body>
        <?php
        echo $this->action('main-menu', 'index', 'default');
        echo $this->action('menu', 'index', 'admin');
        echo $this->layout()->content; 
        ?>
    </body>
</html>
